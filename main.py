from planner import Planner

def show_menu():
    """
    TODO: print the menu options, e.g.:
    1. Add task
    2. View all tasks
    3. View prioritized tasks
    4. Mark task complete
    5. Remove task
    6. Export report
    7. Quit
    """
    pass

def main():
    """
    TODO: 
    1. Create a Planner instance (which should load existing tasks).
    2. Loop: show_menu(), get user's choice via input().
    3. Based on choice, call the matching Planner method, collecting
       any additional input needed (title, deadline, priority, task id).
    4. Print confirmation or results after each action.
    5. Exit the loop cleanly on 'quit'.
    
    Remember your recurring weakness: don't put your loop's exit
    return/break logic buried inside unrelated branches. Structure
    the loop so the exit condition is obvious at a glance.
    """
    pass

if __name__ == "__main__":
    main()